import SearchBar from "@/components/main/nav";
import { AllTickets } from "@/components/main/allTickets";

const Main = () => {
  return (
    <section>
      <SearchBar />
      <AllTickets />
    </section>
  );
};

export default Main;
